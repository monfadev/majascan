name: majascan_example
description: Demonstrates how to use the majascan plugin.
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  majascan:
    path: ../

flutter:
  uses-material-design: true
